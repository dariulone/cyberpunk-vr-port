[33mcommit f5e59d7e81a35ccf71da1e75b34483335e908174[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmain[m[33m, [m[1;33mtag: [m[1;33m0.1.1[m[33m, [m[1;31morigin/main[m[33m, [m[1;31morigin/HEAD[m[33m)[m
Author: dariulone <dariulone@gmail.com>
Date:   Mon Aug 3 12:01:41 2026 +0300

    0.1.1: fix ResizeBuffers, remove the DLSS resolution override and matrix hook
    
    ResizeBuffers re-derived the swapchain height from the runtime's recommended aspect instead of
    taking the launcher's pick -- a 2560x2848 selection came out as 2560x2682. Both it and
    ResizeBuffers1 now use the launcher size verbatim.
    
    xr_dlss_res_override and xr_dlss_matrix_hook are deleted, along with xr_dlss_slot_mode and
    xr_dlss_log_stride which only fed them. 553 lines; both were AER-era and shipped off.

[1mdiff --git a/src/vr/core/swapchain_hooks.cpp b/src/vr/core/swapchain_hooks.cpp[m
[1mindex 5bc6f09..65ed0d3 100644[m
[1m--- a/src/vr/core/swapchain_hooks.cpp[m
[1m+++ b/src/vr/core/swapchain_hooks.cpp[m
[36m@@ -28,7 +28,8 @@[m [mextern "C" UINT GetForcedDisplayModeHeight();[m
 extern "C" UINT GetForcedWindowWidth();[m
 extern "C" UINT GetForcedWindowHeight();[m
 extern "C" int GetMenuMode();[m
[31m-extern "C" UINT GetForcedRenderHeightForAspect();[m
[32m+[m[32m// Nothing in this file may re-derive a size: the swapchain gets the launcher's pick verbatim.[m
[32m+[m[32m// The helper that used to do it is gone entirely -- see HookedResizeBuffers.[m
 [m
 namespace {[m
 using PresentFn = HRESULT(STDMETHODCALLTYPE*)(IDXGISwapChain*, UINT, UINT);[m
[36m@@ -1528,10 +1529,32 @@[m [mHRESULT STDMETHODCALLTYPE HookedSetFullscreenState(IDXGISwapChain* swapChain, BO[m
     return originalFn ? originalFn(swapChain, FALSE, target) : DXGI_ERROR_INVALID_CALL;[m
 }[m
 [m
[32m+[m[32m// THE LAUNCHER'S SIZE, VERBATIM. Nothing is recomputed here.[m
[32m+[m[32m//[m
[32m+[m[32m// This used to take the width from the launcher and RE-DERIVE the height as[m
[32m+[m[32m// launcherWidth / (the runtime's RECOMMENDED render-target aspect), which is not the aspect[m
[32m+[m[32m// that matters. On a Quest 3 it is 1680/1760 = 0.95455, so a 2560x2848 pick came out as[m
[32m+[m[32m//[m
[32m+[m[32m//     ResizeBuffers override: 2560x2848 -> 2560x2682          (2560 / 0.95455 = 2681.9)[m
[32m+[m[32m//[m
[32m+[m[32m// Three things wrong with it. It contradicted the contract written on the function it called[m
[32m+[m[32m// ("only the RENDER override uses this; window/swapchain getters keep the launcher value").[m
[32m+[m[32m// It disagreed with CreateSwapChainForHwnd, which forces the launcher size, so the same[m
[32m+[m[32m// swapchain was created at one shape and resized to another. And the aspect it used is the[m
[32m+[m[32m// wrong one: the rule this port is built on is rect aspect == frustum TANGENT aspect, and for[m
[32m+[m[32m// a Quest 3 that is 1.072369/1.191754 = 0.89982 -- which is what the launcher's 2560x2848[m
[32m+[m[32m// (0.89888) already is. Recomputing replaced a correct height with a panel-shaped one and cost[m
[32m+[m[32m// 3.4 degrees of vertical field: the engine derives tanV = tanH / renderAspect, so 2682 gives[m
[32m+[m[32m// V = 96.66 while the lens wants 100.00 and 2848 gives 100.06.[m
[32m+[m[32m//[m
[32m+[m[32m// It went unnoticed because on a PICO the recommended size is near-square and so is the[m
[32m+[m[32m// tangent aspect, so the two agreed and this was the identity.[m
[32m+[m[32m//[m
[32m+[m[32m// Both of the other things that read that aspect -- the DLSS resolution override and the DLSS[m
[32m+[m[32m// projection-matrix injection -- were deleted with it, so there is no second path back in.[m
 HRESULT STDMETHODCALLTYPE HookedResizeBuffers(IDXGISwapChain* swapChain, UINT bufferCount, UINT width, UINT height, DXGI_FORMAT newFormat, UINT flags) {[m
     const UINT forcedWidth = GetForcedSwapchainWidth();[m
[31m-    //const UINT forcedHeight = GetForcedSwapchainHeight();[m
[31m-    const UINT forcedHeight = GetForcedRenderHeightForAspect();[m
[32m+[m[32m    const UINT forcedHeight = GetForcedSwapchainHeight();[m
 [m
     const UINT outWidth = forcedWidth != 0 ? forcedWidth : width;[m
     const UINT outHeight = forcedHeight != 0 ? forcedHeight : height;[m
[36m@@ -1545,11 +1568,14 @@[m [mHRESULT STDMETHODCALLTYPE HookedResizeBuffers(IDXGISwapChain* swapChain, UINT bu[m
     return originalFn ? originalFn(swapChain, bufferCount, outWidth, outHeight, newFormat, flags) : DXGI_ERROR_INVALID_CALL;[m
 }[m
 [m
[32m+[m[32m// Same rule as HookedResizeBuffers above, and for the same reasons: the launcher's size, verbatim.[m
[32m+[m[32m// The two must agree -- a swapchain that supports ResizeBuffers1 goes through this one instead, so[m
[32m+[m[32m// fixing only the other would leave the bug alive on exactly the runtimes that take this path.[m
 HRESULT STDMETHODCALLTYPE HookedResizeBuffers1(IDXGISwapChain3* swapChain, UINT bufferCount, UINT width, UINT height, DXGI_FORMAT format, UINT flags, const UINT* creationNodeMask, IUnknown* const* presentQueue) {[m
     const UINT forcedWidth = GetForcedSwapchainWidth();[m
[31m-    //const UINT forcedHeight = GetForcedSwapchainHeight();[m
[31m-    const UINT forcedHeight = GetForcedRenderHeightForAspect();[m
[31m-    [m
[32m+[m[32m    const UINT forcedHeight = GetForcedSwapchainHeight();[m
[32m+[m
[32m+[m
     const UINT outWidth = forcedWidth != 0 ? forcedWidth : width;[m
     const UINT outHeight = forcedHeight != 0 ? forcedHeight : height;[m
     if (outWidth != width || outHeight != height) {[m
[1mdiff --git a/src/vr/core/vr_core.cpp b/src/vr/core/vr_core.cpp[m
[1mindex 13fa4b1..711d6df 100644[m
[1m--- a/src/vr/core/vr_core.cpp[m
[1m+++ b/src/vr/core/vr_core.cpp[m
[36m@@ -87,12 +87,6 @@[m [mstruct LiveControls {[m
     volatile float xrMenuFov;[m
     volatile float xrMenuFollowDeg; // head-vs-panel yaw offset (deg) that starts the lazy menu re-center[m
     volatile int xr3DofMovement;[m
[31m-    // BOTH DLSS KNOBS DEFAULT OFF -- they are AER-era machinery, see the note on[m
[31m-    // ApplyDLSSResolutionOverride. Kept because they are the only live way back in.[m
[31m-    volatile int xrDLSSMatrixHook;   // 1 = inject our projection into the DLSS state + remap slots[m
[31m-    volatile int xrDLSSSlotMode;[m
[31m-    volatile int xrDLSSLogStride;[m
[31m-    volatile int xrDLSSResOverride;  // 1 = force our dims into the DLSS resolution struct[m
     // 1 = this is still the first launch, i.e. the shipped UserSettings.json has not been[m
     // installed yet. ApplyFirstLaunchGameSettings CLEARS it to 0 once it has, so a player's own[m
     // tuning is never overwritten twice. See the note there.[m
[36m@@ -225,10 +219,6 @@[m [mstatic void EnsureLiveControlFileExists() {[m
     fprintf(file, "xr_menu_fov=65.0\n");[m
     fprintf(file, "xr_menu_follow_deg=60.0\n");[m
     fprintf(file, "xr_3dof_movement=0\n");[m
[31m-    fprintf(file, "xr_dlss_matrix_hook=0\n");[m
[31m-    fprintf(file, "xr_dlss_slot_mode=0\n");[m
[31m-    fprintf(file, "xr_dlss_log_stride=600\n");[m
[31m-    fprintf(file, "xr_dlss_res_override=0\n");[m
     fprintf(file, "first_launch=1\n");[m
     fprintf(file, "xr_motion_predict_ms=0.0\n");[m
     fprintf(file, "xr_stereo_scale=1.0\n");[m
[36m@@ -440,10 +430,6 @@[m [mstatic void PollLiveControls() {[m
     float xrPitchScale = 1.35f;[m
     int xrSyncSequential = 1;[m
     int xr3DofMovement = 0;[m
[31m-    int xrDLSSMatrixHook = 0;[m
[31m-    int xrDLSSSlotMode = 0;[m
[31m-    int xrDLSSLogStride = 600;[m
[31m-    int xrDLSSResOverride = 0;[m
     int xrFirstLaunch = 1;[m
     float xrMotionPredictMs = 0.0f;[m
     float xrStereoScale = 1.0f;[m
[36m@@ -577,27 +563,6 @@[m [mstatic void PollLiveControls() {[m
             xr3DofMovement = intValue;[m
             continue;[m
         }[m
[31m-[m
[31m-        if (sscanf_s(line, "xr_dlss_matrix_hook=%d", &intValue) == 1 ||[m
[31m-            sscanf_s(line, "xr_dlss_matrix_hook = %d", &intVal