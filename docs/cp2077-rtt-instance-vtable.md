---
name: cp2077-rtt-instance-vtable
description: live RTT-camera instance vtable dumped in-process; target@0x1E0=null is the confirmed no-op crux; RTT-specific virtual cluster identified
metadata:
  type: reference
---

CP2077 native True-Stereo (2026-06-08): dumped the live `entRenderToTextureCameraComponent` instance vtable in-process via new red4ext native fn `DumpRTTCamVtable()` (CET: create comp + AddComponent, then call it; writes rttcam_vtable_dump.txt). Instance size 2576 (0xA10).

**Instance layout confirmed** (our CET-set values landed): isEnabled byte @0x8b; fov f32 @0x128 (=90.0); resolutionWidth/Height u32 @0x258/0x25c (=1024/1024). **`dynamicTextureRes` (raRef) @0x1E0 = 0** => this is the confirmed reason the component is a **no-op**: a render-to-texture camera with no target writes nowhere. Pointers: +0xa0 -> rva 0x3075268, +0x120 -> rva 0x2b03a70 (camera class desc) with fov right after at +0x128, +0x280 -> rva 0x3008ec8.

**Vtable**: many slots = 0x14014A700 (AK StartProfileThreadUsage stub = unimplemented virtual). Generic IComponent slots (0x9c4600/0x496010/0x19a9b0). **RTT-specific override cluster** (unique to this class): 0x23E7518, 0x23E741C, 0x23E759C, 0x23E72EC, 0x23E7384, 0x23E7650, 0x23E73C8 and 0x23F467C, 0x23F34A8, 0x23F374C, 0x23F3450, plus class-method region 0x18Dxxxx/0x18Exxxx (0x18D9130, 0x18E3B70, 0x18EE590, 0x18EF760...). One of these reads +0x1E0 and is the attach/render driver to reverse. Refines [[cp2077-rtt-no-drivable-api]]; the crux is now pinned to "supply a valid DynamicTexture at +0x1E0", not driver-API discovery.

**CRUX TEST NEGATIVE (2026-06-08)**: new native fn `TryCreateRTTTarget(w,h)` did `rtti->GetClass("DynamicTexture")->CreateInstance` (props width@0x40/height@0x44 set), wrote the instance ptr into comp+0x1E0 (before=0->after=ptr confirmed). Component isEnabled=1, unique res 1280x1152. RT-diag (windowed) showed **NO 1280x1152 cluster** -> the render system does NOT render our component even with a target assigned. Conclusion: a bare reflected DynamicTexture has no GPU backing, and/or the render-system only iterates RTT-cams wired by their C++ owning system (holocall/CCTV) -- not arbitrary script-added ones. ALL cheap/script/in-process levers for a 2nd native world view are now empirically closed. Native true-stereo here = multi-week C++ render-system reverse (DynamicTexture GPU alloc + system registration), with real risk it doesn't generalize to "full world from arbitrary camera as a 2nd simultaneous view".

## LoadDynamicTexture driver chain (2026-06-08)
String 'RenderToTextureCameraComponent/LoadDynamicTexture' @0x14307BF08 -> method sub_141C9F5B0 (job-scope wrapper) -> worker sub_141C9F660 -> job body sub_141C9F700. Body GATE: (comp[0x88] & 2) && (comp[0x8b]=isEnabled || comp[0x9FE]); binds resource from job payload (res+40 hash / res+48 handle) into comp+0x1E8, then sub_1412BAACC(comp, format@payload+32) = RT create/register. CALLER sub_1423E7768 (RTT-specific): if (!comp[0x1E8] && comp[0x1E0]) { sub_140233B68(comp+0x1E0,&res); ... LoadDynamicTexture(res) } = engine AUTO-LOAD when ref set + handle empty. KEY: comp+0x1E0 (m_dynamicTextureRes) is a resource-REF (raRef hash), NOT a raw ptr -> our CreateInstance write was wrong type. Need a real GPU-backed DynamicTexture resource at 0x1E0. GPU alloc likely in sub_1412BAACC (next). Refines [[cp2077-rtt-no-drivable-api]].

## FULL native chain mapped (2026-06-08) — MECHANISM COMPLETE
sub_1412BAACC (RT create/register): if comp[0x1E8] handle loaded -> submit texture (sub_1420FAB9C); if sub_140665054(comp) -> **register camera with render system**: sub_1423E4658(renderSys, comp+0x120) [renderSys via comp[10]->+184/+192 ptr chain] OR renderSys.vtable[56](.,.,format). After register, render system draws scene from camera into the DynamicTexture each frame. 
Resource load: comp+0x1E0 ref -> sub_140233B68 -> sub_140233C08 -> sub_1401DE50C(qword_143426E58 = RESOURCE DEPOT, out, token) = async depot resolve. So comp+0x1E0 = depot resource TOKEN (raRef hash), needs a **depot-resident GPU-backed DynamicTexture**, not a bare CreateInstance obj (confirms why crux test failed). 
CONVERGED: native true-stereo mechanism fully mapped & auto (load+register+render). SINGLE remaining task = produce a depot-resident DynamicTexture. Two impl routes: (A) author DynamicTexture resource via ArchiveXL (supportable, if cookable); (B) C++ create GPU-backed DynamicTexture + hook depot resolve sub_1401DE50C to serve it for our token (needs finding engine DynamicTexture-create, e.g. PoolRenderDynamicTexture @str 0x142C14D08). Also gate comp[0x88]&2 must be set + component must tick.

## Virtual-camera pair ALSO negative (2026-06-08)
entVirtualCameraComponent (source: virtualCameraName@0x1e0, resW/H@0x1e8/ec, isEnabled@0x8b, NO dynamicTextureRes -> engine-internal texture by name) added to player alone = no render. Added WITH entVirtualCameraViewComponent (sink: virtualCameraName@0x140, targetPlaneSize@0x148) referencing same name = STILL no 1280x1152 MRT cluster in RT-diag. Both script-added to player. CONCLUSION: every script-reachable camera path (RTT cam +/- CreateInstance target, virtual_camera.ent spawn, entVirtualCameraComponent source-only, source+sink pair) fails to self-render -- the engine only drives these in proper context (world-placed TV/monitor entities, holocall quest systems), not when script-injected onto the player. No script-reachable native 2nd-world-view exists. Native true-stereo's remaining piece = reproduce the C++ render-system driving (GPU DynamicTexture create + depot register + system tick) in our own code = deep render-internal reverse, no clean callable entry.

## Reproduction primitives mapped (2026-06-08, R2)
Camera register: sub_1423E4658(renderSys, &cameraDesc) -> lock@+0x48, add desc to container @renderSys+0x50 (sub_14207E91C), unlock. RenderSys reachable from comp[0x50] chain (->+184/+8/+456) or global qword_14342B1A0 (service-locator key). 
GPU texture alloc: sub_14291A4D4(ctx, &{textureObj}, w=r8d, h=r9d, flag) = 'ResizeDynamicTexture' -> builds render-cmd (vtable off_1431D8548, sub_142918114) + submits to render thread (sub_142910480). Thread-safe (enqueues). This is the GPU-backing allocator for a DynamicTexture. 
Reproduction sketch: CreateInstance(DynamicTexture) -> sub_14291A4D4 to alloc GPU @ eye-res -> place handle into a camera-view desc (right-eye matrices, layout known: quat@0x70/fov@0x80/nearfar@0xA0/view@0xB0/pos@0xE0/proj@0x1B0 per [[cp2077-perview-camera-layout]]) -> sub_1423E4658 register into renderSys+0x50. Remaining unknowns: exact textureObj layout & ctx for resize call; cameraDesc construction; gate comp[0x88]&2. Next: decompile a clean CALLER of sub_14291A4D4 to copy the ctx/textureObj setup.

## Render texture-manager vtable (2026-06-08)
ResizeDynamicTexture (sub_14291A4D4) lives in vtable head off_142C11958 at slot 80 (offset +0x280). Vtable constructed by sub_141049C10(+18) / sub_141049E6C(+19) -> these store the render-texture-manager instance into a global (next: trace where). To create a GPU DynamicTexture from our plugin we must obtain that instance and call vtable[80]. R2 reconnaissance now essentially complete: full consumption chain + all 3 reproduction primitives (GPU alloc vtable[80]@off_142C11958, camera register sub_1423E4658->renderSys+0x50 container, render-system reachable via comp[0x50] chain / qword_14342B1A0) mapped. Remaining = pure C++ implementation: obtain the 2 render-object globals, assemble create-tex/build-desc/register/grab sequence on render thread. Multi-week, fragile (raw render-internal vtable calls), risk of non-generalization. User is hitting IDA-navigation friction -> next move is impl smoke-test or pause, not more blind decompiles.
