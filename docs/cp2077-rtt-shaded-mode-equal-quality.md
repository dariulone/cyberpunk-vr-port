---
name: cp2077-rtt-shaded-mode-equal-quality
description: DECISIVE positive for option-B vehicle choice — entRenderToTextureMode={Shaded,GBufferOnly}; RTT camera natively supports a FULL shaded view (no reduced renderstage_rtt_* unlike the mirror), so it IS the equal-quality right-eye vehicle
metadata:
  type: project
---

IDA enum decode (2026-06-09, no decompile/disasm — byte-decoded registration fns):

**entRenderToTextureMode** (type-name string @0x142d022c0; registrar sub_14153A194, CName hash 0xEB066ADA5FA1B5A9) has exactly TWO members via AddOption:
- value 0 = **"Shaded"** (@0x142d022b4)
- value 1 = **"GBufferOnly"** (@0x142d022a8)

=> an RTT camera can natively render a **fully shaded/lit** view, not just a G-buffer. The component's renderingMode field is at comp+0x290.

**entRenderToTextureFeatures** (type-name @0x142cfecc0; registrar sub_14152F038) is registered as a size-8 bitmask type (r9d=2/r8d=8, no AddOption member loop) — feature flags (post/AA/transparency toggles) live as bits, names not enumerated in the registrar. Companion type entRenderToTextureFeaturesPlatform (@0x142ca67a8).

**Render-stage taxonomy check:** there is NO dedicated reduced `renderstage_rtt_*` / `monitor_*` stage set. Contrast the mirror ([[cp2077-mirror-planar-reflection-view]]) which has its OWN cut-down stages (renderstage_planar_reflection, depthprepass_mirror_opaque_notxaa, opaque_notxaa). => when an RTT camera renders in Shaded mode it goes through the STANDARD shaded render-stages (full pass-set), via the Type-A view-item ([[cp2077-view-item-layout]] vtable 0x2AC8688) and its own independent FG-build ([[cp2077-per-view-independent-fgbuild]]).

**DECISIVE verdict (answers user's "проверь камеры"):** unlike the mirror, the RTT camera is NOT inherently reduced — Shaded mode = full lit scene through standard stages. So the RTT camera IS the equal-quality simultaneous-2nd-view vehicle the user requires. This validates pursuing **option-B**: spawn an entity carrying an RTT camera (renderingMode=Shaded, features maxed, res set to full eye res via comp+0x258/0x25C — preview default was only 720x720), render-attached & active on the player → engine fan-out renders it as a simultaneous full-shaded right-eye view.

**Stage-taxonomy corroboration (full renderstage_ string list decoded):** there is NO renderstage_rtt_/_offscreen_/_capture_/_monitor_ family. The ONLY reduced/special stage families are mirror/planar-specific: _notxaa (renderstage_transparent_notxaa, opaque_notxaa), _mirror (depthprepass_mirror_opaque_notxaa, maskstencil_opaque_notxaa) + renderstage_planar_reflection. The full standard pool (gbuffer_regular, lighting, transparent[TXAA], distortion, post_gbuffer, velocitybuffer, subsurface_emissive, hair_*, skin_translucency, screenspace_vfx, post_feedback_color, depth_prepass, dynamic_decals, etc.) is GENERIC, not mirror-gated. => a Shaded RTT view draws from the same standard stage pool as the main eye; the mirror's reduction is a deliberate mirror-only choice, NOT a limitation imposed on RTT. Equal-quality parity is therefore architecturally available; which feature bits are ON by default for a spawned RTT cam is a CONFIGURATION concern for the spawn test, not an architectural blocker.

Feature-bit ENUMERATION static-dead-end (don't retry): entRenderToTextureFeatures reflects via generated GetType thunks (sub_141DDC050 features-type, sub_141DDC040 component-type) wired into RED4's RTTI via indirect descriptor tables — get_callers/get_xrefs on them and on the type globals (features=0x1444029A0, component=0x144402C78) only return the registrars/thunks, never a property-describe with named bit offsets. Bit semantics live only in the downstream/virtual FG view-build branch logic (not cheaply static). Settled: exact bit names are not statically extractable; resolve at spawn-test time by dumping the live comp's features field.

Still-open (honest): (1) exact Features-bit parity with main eye (post/AA/transparency) — not enumerated statically; (2) the persistent WALL = getting it render-active on the player (render-attach lifecycle / handle@comp+0x1E8 from resref@+0x1E0), only a runtime test answers whether the engine ticks update on a player-attached RTT cam — see [[cp2077-loaddyntex-handle-producer]] [[cp2077-attach-is-render-thread-virtual]]. User's hypothesis (fullscreen surveillance cam = view SWAP not 2nd view) likely true for that UI, but irrelevant: persistent in-world RTT producers render simultaneously and the Shaded mode proves full-quality capability regardless.

**SPAWN TEST IMPLEMENTED (2026-06-09, built+deployed 21:09):** new native `SpawnRTTCamOnPlayer(w,h,resLo,resHi)` in main.cpp (after PollRTTHandle): CreateInstance entRenderToTextureCameraComponent + InitializeProperties, set owner=player, renderingMode@0x290=Shaded(0), res@0x258/0x25C, isEnabled@0x8B=1, optional resref@0x1E0, then PushBack into player->components — CAPACITY-GUARDED (only if Size<Capacity, else abort) to avoid reallocating the live DynArray under the engine. Issues NO engine render/attach call (the DriveRTTViewCreate deadlock came from synchronous render-view-create on the script thread; avoided here). Observe via existing `PollRTTHandle()` (extended to log flag88/subVt@+0x120/view-pair@+0x268). Run: CET `SpawnRTTCamOnPlayer(2048,2048,0,0)` then `PollRTTHandle()` across several frames. Decisive outcomes: handle@+0x1E8 resolves OR view-pair@+0x268 allocs => engine ticks/attaches a player cam (option-B viable, lever found); both stay null after frames => mere components-array presence is insufficient, need real component render-attach enroll (escalate to engine-thread append / appearance-refresh trigger). Logs: bin\x64\rttcam_spawn.txt + rttcam_poll.txt. NOT YET RUN by user.
