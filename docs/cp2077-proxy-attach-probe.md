---
name: cp2077-proxy-attach-probe
description: BUILT+DEPLOYED 2026-06-09 — read-only F7 probe hook on render-proxy attach sub_140912294 (rva 0x912294) to capture the attach DISPATCHER (the iterator to piggyback for the right-eye cam). Awaiting user run + log.
metadata: 
  node_type: memory
  type: project
  originSessionId: a4faea66-21d0-4884-a9b4-10c3d5515440
---

**Why:** task = find the per-frame render-scene attach iterator that virtually dispatches sub_140912294 (the proxy attach/init that calls sub_140912A18 + sub_142211654 -> init-driver sub_14012F190 = GPU-backing). sub_140912294 has ZERO direct callers (pure virtual dispatch); EVERYTHING above it is virtual; static IDA can't find indexed vtable calls (decompile banned [[ida-no-decompile-rule]], strings time out). So capture the dispatcher at RUNTIME. This is the execution mechanism for [[cp2077-native-stereo-roadmap]] vector 2 (render-thread attach-hook), the user's chosen path.

**What was built (dxgi_proxy.cpp, 3 edits, built+deployed 22:31):**
- `OnProxyAttachProbeCallback` + `InstallProxyAttachProbeHook` (rva 0x912294). Prologue `48 89 5C 24 10 48 89 74` (`mov [rsp+10h],rbx` = clean 5-byte E9 boundary, same as SceneDrv) => proven NodeExec/SceneDrv trampoline verbatim, read-only, no call-original thunk. PatchCodeSafely (thread-suspend) + F7 quiescent manual install.
- Callback: first 8 calls log `[ProxyAttachProbe] call #N proxy=0x.. proxyVtable=0x..` + `DumpRenderCallStack("ProxyAttach")` (raw-stack scan -> topmost in-module `ida=0x..` return site = the DISPATCHER's call to sub_140912294). Every 256 calls logs `total=.. at frame=..` (firing-rate signal).
- F7 install block in DrainViewCaptures gated by g_proxyAttachHookDone (independent of the dead producer hooks; F7 also installs those but they only crash when ARMED via F11 — do NOT press F11/F9/F12 for this test).

**RUN (user):** in-world, quiet spot -> press **F7** (look for `ProxyAttach probe installed at .. (rva=0x912294)`), then walk around / open inventory (triggers proxy attaches) -> send `bin\x64\CyberpunkVRProxy.log`.

**DECODE:** (1) the `StackTrace[ProxyAttach]` raw-scan top in-module `ida=` addr = the attach dispatcher -> get_callers/xref it in IDA to climb to the per-frame iterator. (2) `proxyVtable` RVAs show which proxy types attach (watch for RTT proxy 0x3009130). (3) `total` growth vs frame: steady every frame = per-frame iterator (clean piggyback); only bursts on inventory/stream = event-driven attach (we'd trigger it, not piggyback) — changes the injection design in task #3.

**RUN #1 RESULT (2026-06-09, cyberpunkvrport.log) — DECISIVE, premise falsified:** probe INSTALLED fine (frame 4022, abs 0x7FF6E1492294, base 0x7FF6E0B80000, prologue matched, SUCCESS) but **fired ZERO times** across ~1400 frames (4022→5413) of normal in-world gameplay. CRUCIALLY the same window had `[FG-BUILD] builds/frame=2` from frame 4500–5400+ (~900 frames = a 2nd full view rendering every frame, inventory open) yet sub_140912294 NEVER fired. =>
- **sub_140912294 (render-proxy attach) is a ONE-TIME setup event, NOT a per-frame iterator.** The 2nd view renders every frame via the FAN-OUT (sub_140167EB4) WITHOUT re-calling attach. The "per-frame attach iterator to piggyback" premise from [[cp2077-attach-is-render-thread-virtual]] is EMPIRICALLY WRONG — there is no per-frame attach loop.
- **Why it stayed silent:** builds/frame went 1→2 AFTER install (inventory opened ~frame 4400 post-F7), but the preview cam's handle was GPU-backed earlier (first inventory open BEFORE F7); reopening only toggles isEnabled/view-active ([[cp2077-rttcam-isenabled-is-the-flag]]), it does NOT re-attach. So no fresh attach happened while armed.
- **POSITIVE:** builds/frame=2 is live proof the engine renders 2 full views/frame via the fan-out once a 2nd view exists. So the wall is unchanged (one-time handle/attach), but the fan-out will render our 2nd view for free once backed.

**RUN #2 RESULT (2026-06-09): proxy-attach probe AGAIN fired 0× — pivoted instrument.** Probe installed (frame 3123, SUCCESS) but zero fires across ~2900 frames. Couldn't even confirm inventory opened (FgBuild hook didn't install this session so no builds/frame signal; MenuMode doesn't log state). Two blind round-trips on sub_140912294 = it's either the wrong branch (GPU-backing also reachable via sub_140913588→sub_14012F190, NOT through sub_140912294) or never triggered. STOP iterating on the one-time proxy attach.

**NEW INSTRUMENT (built+deployed 22:52): COMPONENT-UPDATE probe on sub_1423E759C** (rva 0x3E759C, prologue `mov [rsp+8],rbx` clean 5-byte). This is the documented per-frame handle producer for entRenderToTextureCameraComponent (vtable 0x14307BFD0 slot 12, drives LoadDynamicTexture -> handle@+0x1E8, [[cp2077-loaddyntex-handle-producer]]). Callback `OnCompUpdateProbeCallback` FILTERS to vtable==base+0x307BFD0 (our component type only — no noise), counts `g_compUpdRttTotal` (SELF-CONFIRMING), dumps first 8 stacks (`[CompUpdateProbe]` + `StackTrace[CompUpdate]`). Advantages over the proxy-attach probe: (a) ticks EVERY FRAME the cam is active (inventory open) = no fresh-attach timing needed; (b) self-confirms whether the cam ticks at all; (c) its dispatcher = the per-frame component-tick loop = a real piggyback injection point for our right-eye component. Both probes now install on F7.

**RUN #3 RESULT (2026-06-09): CompUpdate probe DID NOT INSTALL — infra bug found + fixed.** Log showed sceneDrv (block1) + ProxyAttach (block2) installed on F7 but CompUpdate (block3) + FgBuild (block4) silently skipped — SAME cutoff across 3 sessions. ROOT CAUSE: each F7 install block re-polled `GetAsyncKeyState(VK_F7)` independently; the earlier blocks' `PatchCodeSafely` (suspend-all-threads) eats enough wall-clock that later blocks read the key as released => skip. FIX (built+deployed 23:00, md5 c28b57a1): latch F7 ONCE at top of DrainViewCaptures (`f7Install`), all 4 install blocks use the latched bool => one press installs everything. NOTE for future probe adds: always gate F7/F8 install blocks on a single latched read, never re-poll per block.

**RUN #4 RESULT: `CompUpdate probe result: FAILED`** — my RVA arithmetic bug. sub_1423E759C − base 0x140000000 = **0x23E759C**, I wrote 0x3E759C (dropped the leading 2) => base+0x3E759C = wrong addr, prologue mismatch => FAILED. (The latch fix worked — the block now RUNS.) FIXED all 4 occurrences to 0x23E759C, rebuilt+deployed 23:03 (md5 7fff6a3f). Prologue at 0x1423E759C confirmed `48 89 5C 24 08 57 48 83` = matches kExpectPrologue. LESSON: rva = ida_addr − 0x140000000 (sub_142… → 0x2…, not 0x…).

**RUN #5 RESULT (2026-06-09) — DECISIVE PIVOT:** all hooks installed (latch worked: sceneDrv+ProxyAttach+CompUpdate SUCCESS@rva 0x23E759C+FgBuild, frame 2614). `builds/frame` went 1→2 at frame 4200 and held **2 for ~5400 frames** (4200→9600, inventory open = a 2nd full view rendering every frame). Across all 5400 frames: **CompUpdate probe (filtered to vtable 0x14307BFD0) = 0 fires AND ProxyAttach probe (UNFILTERED) = 0 fires.** SceneDrv kept firing (72 passes/frame) = infra alive. => **the inventory 2nd view renders per-frame via the VIEW FAN-OUT, NOT via any RTT-cam proxy-attach (sub_140912294) or RTT-component-update (sub_1423E759C).** The 35-session premise "inventory preview = entRenderToTextureCameraComponent whose handle@+0x1E8 we must create" is challenged: the per-frame 2nd-view render never touches those fns. (ProxyAttach=0 is unfiltered = solid; CompUpdate=0 is filtered to 0x14307BFD0 so slightly less certain, but combined the picture is strong.) The right-eye vehicle is a fan-out VIEW, not an RTT-cam component tick.

**NEXT (NO rebuild — existing tool): F10 FG-DUMP.** OnFGBuildCallback already has an F10-armed dump (g_fgDumpArm, kFgDumpMax=6) that logs arg0(FG-ctx,0x140B)+arg1(job-ctx,0xA0B) memory of the next 6 FG-build hits. During inventory (2 builds/frame) 6 hits = both the main (Type-B job-ctx vtable rva 0x2AC8668) and the 2nd/preview (Type-A vtable rva 0x2ABC878) builds. arg1+0x00 = job-ctx vtable => instantly tags which build is the 2nd view; its arg0/arg1 memory carries the view reference to replicate for the right eye. RUN: F7 (installs FgBuild) → open inventory (builds/frame=2) → press **F10** → wait ~2s → send log; I diff the 6 [FG-DUMP] blocks. See [[cp2077-view-fanout-submit-chain]] [[cp2077-per-view-independent-fgbuild]].

**(superseded run instr) RUN #5:** fresh launch → in-world → press **F7** ONCE → confirm `CompUpdate probe result: SUCCESS` → open inventory, leave open ~5s → send `cyberpunkvrport.log`. Decode unchanged: `[CompUpdateProbe] RTT total>0` => cam ticks, topmost in-module `ida=` in `StackTrace[CompUpdate]` = per-frame RTT-component-tick dispatcher (injection point). total==0 with inventory open => preview cam isn't this vtable (decisive pivot). Do NOT press F11/F9/F12.
